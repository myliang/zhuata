# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://jashkenas.github.com/coffee-script/
# $ ->
#   paginates = $('#js-paginate-chapters').paginate(
#     init_load: false
#     after: (je, data)->
#       je.prev().html(data.content)
#       je.prev().prev().html(data.title)
#   )
#   $('#js-chapters li a').on 'click', (event)->
#     page = @href.split('?')[1].split('=')[1]
#     paginates[0].goto(parseInt(page))
#     $('#js-chapters').hide()
#     $('#js-chapter-content').show()
#     # event.preventDefault()
#     false
#
